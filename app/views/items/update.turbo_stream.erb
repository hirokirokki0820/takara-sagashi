<%= turbo_stream.replace @item %>

<%# 景品一覧のカウント（累計・ハズレ数）を更新する %>
<%= turbo_stream.update "item-total-count", @post.items.count %>
<%= turbo_stream.update "item-lose-count", @post.items.where(lose: true).count %>

<%= turbo_stream_flash %>
